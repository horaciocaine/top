
<?php if (count($notifications = $this->notifications())): ?>
  <div class="notifications">
    <ul>
      <?php foreach ($notifications as $notification): ?>
        <li><?php echo $this->escape($notification); ?></li>
      <?php endforeach; ?>
    </ul>
  </div>
<?php endif; ?>
